type REG = usize;
pub enum IR {
    STOREREG(REG, REG),
    STOREIMM(REG, i128),
    ADDREG(REG, REG),
    ADDIMM(REG, i128),
    SUBREG(REG, REG),
    SUBIMM(REG, i128),
    MULREG(REG, REG),
    MULIMM(REG, i128),
    DIVREG(REG, REG),
    DIVIMM(REG, i128),
    MODREG(REG, REG),
    MODIMM(REG, i128),
    LSHIFTREG(REG, REG),
    LSHIFTIMM(REG, i128),
    RSHIFTREG(REG, REG),
    RSHIFTIMM(REG, i128),
    LTREG(REG, REG),
    LTIMM(REG, i128),
    GTREG(REG, REG),
    GTIMM(REG, i128),
    LTEQREG(REG, REG),
    LTEQIMM(REG, i128),
    GTEQREG(REG, REG),
    GTEQIMM(REG, i128),
    EQREG(REG, REG),
    EQIMM(REG, i128),
    NTEQREG(REG, REG),
    NTEQIMM(REG, i128),
    RETURNREG(REG),
    RETURNIMM(i128),
    LABEL(String),
    JMP(String),
}
