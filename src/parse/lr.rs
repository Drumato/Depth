/*
trait SyntaxDefinition {
    fn ltoken(&self) -> Token;
    fn pattern(&self) -> Vec<Token>;
}
pub struct SyntaxDefinition {
    pub ltoken: Token,
    pub pattern: Vec<Token>,
}

pub struct Grammar {
    syntax: Vec<SyntaxDefinition>,
    start: Token,
}
*/
